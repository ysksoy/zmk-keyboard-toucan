# Mac/Windows切り替え対応ガイド

## 概要
このキーボードは、MacとWindowsの両方で使用できるように設定されています。
ADJレイヤーからOS切り替えが可能です。

## OS切り替え方法

### Macモードに切り替え
1. NAVレイヤーとSYMレイヤーを同時に有効化してADJレイヤーにアクセス
2. 左下の**Z**キーの位置（ADJレイヤーでは`to MAC`）を押す

### Windowsモードに切り替え
1. NAVレイヤーとSYMレイヤーを同時に有効化してADJレイヤーにアクセス
2. 左下の**X**キーの位置（ADJレイヤーでは`to WIN`）を押す

## レイヤー構成

- **Layer 0 (BASE/MAC)**: Macモード用のベースレイヤー（デフォルト）
- **Layer 1 (WIN)**: Windowsモード用のベースレイヤー
- **Layer 2 (NAV)**: ナビゲーション/数字レイヤー
- **Layer 3 (SYM)**: シンボルレイヤー
- **Layer 4 (ADJ)**: 調整レイヤー（NAV + SYMで自動的に有効化）

## OS別のキーマッピング差異

### Macモード（Layer 0 - デフォルト）
- **左親指（外側）**: `Cmd + Shift + かな` (IME切り替え)
- **右親指（外側）**: `Alt + 英数` (IME切り替え)

### Windowsモード（Layer 1）
- **左親指（外側）**: 長押し `Ctrl` / タップ `Shift + LANG2`
- **右親指（外側）**: 長押し `Alt` / タップ `LANG1`

## コンボキー（同時押しショートカット）

### Mac専用コンボ
| キー位置 | 機能 | キーコード |
|---------|------|-----------|
| D + F | ブラウザで戻る | `Cmd + [` |
| G + F | ブラウザで進む | `Cmd + ]` |
| Q + W | スクリーンショット | `Cmd + Shift + 4` |
| F + G | ブラウザリロード | `Cmd + R` |
| N + M | Control Center | `Ctrl + ↑` |
| S + D | デスクトップを左へ切り替え | `Ctrl + ←` |
| L + K | デスクトップを右へ切り替え | `Ctrl + →` |

### Windows専用コンボ
| キー位置 | 機能 | キーコード |
|---------|------|-----------|
| D + F | ブラウザで戻る | `Alt + ←` |
| G + F | ブラウザで進む | `Alt + →` |
| Q + W | スクリーンショット | `Win + Shift + S` |
| F + G | ブラウザリロード | `F5` |
| N + M | タスクビュー | `Win + Tab` |
| S + D | 仮想デスクトップを左へ切り替え | `Win + Ctrl + ←` |
| L + K | 仮想デスクトップを右へ切り替え | `Win + Ctrl + →` |

### 共通コンボ（Mac/Windows両対応）
| キー位置 | 機能 | キーコード |
|---------|------|-----------|
| I + O | マイナス記号 | `-` |
| J + K | 右クリック | マウス右クリック |
| H + J | 左クリック | マウス左クリック |

## ビルド方法

設定を反映するには、ZMKファームウェアを再ビルドする必要があります：

1. GitHub Actionsで自動ビルド（推奨）
   - このリポジトリをGitHubにプッシュ
   - GitHub Actionsが自動的にファームウェアをビルド
   - Artifactsからファームウェアをダウンロード

2. ローカルでビルド
   ```bash
   # ZMK開発環境が必要です
   west build -b nice_nano_v2 -- -DSHIELD=toucan
   ```

## トラブルシューティング

### OS切り替えができない
- NAVレイヤーとSYMレイヤーを**同時に**押してADJレイヤーにアクセスできているか確認
- ファームウェアが最新版に更新されているか確認

### コンボが動作しない
- 現在のレイヤーを確認（MACレイヤーかWINレイヤーか）
- キーを同時に押すタイミングを調整

### IME切り替えが動作しない（Windows）
- Windowsの日本語キーボード設定で`LANG1`/`LANG2`がIME切り替えとして動作するか確認
- 設定 > 時刻と言語 > 言語と地域 > 日本語 > 言語のオプション

## カスタマイズ

`config/toucan.keymap`を編集することで、さらにカスタマイズできます：
- コンボキーの追加/変更
- レイヤーの追加
- キーマッピングの変更

詳細は[ZMK公式ドキュメント](https://zmk.dev/docs)を参照してください。
